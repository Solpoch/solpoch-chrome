const BASE_URL = "https://extention.server.solpoch.site/api";

export const API_ROUTES = {
  ai: {
    explainSimulationError: `${BASE_URL}/ai/explain-simulation-results`,
    dAppAnalysis: `${BASE_URL}/ai/analyze-dapp-payload`,
  }
}